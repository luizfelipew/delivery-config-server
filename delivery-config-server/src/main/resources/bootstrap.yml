spring:
  application:
    name: delivery-config-server
  cloud:
    config:
      discovery:
        enabled: false

eureka:
  instance:
    hostname: localhost

  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/