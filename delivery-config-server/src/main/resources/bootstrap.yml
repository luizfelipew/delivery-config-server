spring:
  application:
    name: deliveryfacil-config-server
  cloud:
    config:
      discovery:
        enabled: true

eureka:
  instance:
    hostname: localhost

  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/